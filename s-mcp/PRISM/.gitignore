# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Archive directory (deprecated files)
Archive/

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~
.project
.pydevproject
.settings/
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Claude Code
.claude/

# Empty cache directory
cache/

# GROMACS simulation outputs (large binary files)
*.trr
*.xtc
*.tpr
*.edr
*.cpt
*.gro
*.ndx
*.xvg
*.log

# Project specific
test/*
.playwright-mcp/*

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# mypy
.mypy_cache/
.ruff_cache
.qoder
.playwright*
.history
.prism-build/

prism/*/*.backup
prism/*/*/*.backup
.mcp.json
.serena
.*.pid
.claude/skills/__pycache__/fep-visualization-check.cpython-310.pyc
.claude/cclsp.json
.claude/scheduled_tasks.json
*/GMX_PROLIG_FEP/_build/*/*.yaml

# Membrane / ligand build outputs.  Every test build writes under example/ and a
# single membrane system is hundreds of MB (9OMO alone is 1.3 GB), so the tree is
# ignored wholesale.  Input structures are tracked explicitly with `git add -f`.
example/

tests/gxf/

# Generative-model checkpoints (~1 GB) and the upstream source checkouts they
# need.  They live in $PRISM_MODELS_DIR (default ~/.cache/prism/models), which
# is outside the repository; these entries only catch a copy dropped in-tree
# during development.  prism/data/model_weights/manifest.json describes them
# and IS tracked -- only the bytes are ignored.
prism-models/
.prism-models/
prism/data/model_weights/*.ckpt
prism/data/model_weights/*.pt
prism/data/model_weights/*.pth
prism/data/model_weights/*.part
